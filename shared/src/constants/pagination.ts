/** Default page number for offset pagination. */
export const DEFAULT_PAGE = 1

/** Default page size for offset pagination. */
export const DEFAULT_PAGE_LIMIT = 20

/** Maximum allowed page size for offset pagination. */
export const MAX_PAGE_LIMIT = 100

/** Default page size for cursor pagination (high-volume lists). */
export const DEFAULT_CURSOR_LIMIT = 50

/** Maximum allowed page size for cursor pagination. */
export const MAX_CURSOR_LIMIT = 200
